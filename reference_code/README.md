# Reference Code

This directory is for local, ignored checkouts of external research code used during audits.
Only this README is tracked; cloned repositories under this folder are ignored by git.

Current local reference:

- `AlphaNLHoldem/`: unofficial AlphaHoldem-style RLCard implementation, used only
  for audit and isolated benchmark design. Do not copy AGPL code or weights into
  tracked project files without a separate license review.
